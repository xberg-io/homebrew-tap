# AI-Rulez Configuration (migrated to V4 TOML format)
# Documentation: https://github.com/Goldziher/ai-rulez

version = '4.0'
name = 'homebrew-tap'
description = 'Homebrew tap with formulas for kreuzberg-dev CLI tools'
gitignore = true
presets = ['claude', 'codex', 'copilot', 'cursor', 'antigravity']
builtins = ['ruby', 'default-commands']

[[includes]]
name = 'kreuzberg-core'
source = 'https://github.com/xberg-io/ai-rulez.git'
path = 'modules/core'
merge_strategy = 'local-override'
